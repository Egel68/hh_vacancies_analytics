# Архитектура проекта HH.RU Vacancy Analyzer

## 📁 Структура проекта

```
hh_vacancies_analytics/
│
├── core/ # Ядро системы - интерфейсы
│ ├── init.py
│ └── interfaces.py # Абстрактные интерфейсы (SOLID: D, I)
│
├── fetchers/ # Получение данных с API
│ ├── init.py
│ ├── searcher.py # Поиск вакансий (sync/async)
│ └── details_fetcher.py # Получение деталей (sync/async)
│
├── parsers/ # Парсинг и очистка текста
│ ├── init.py
│ └── text_cleaner.py # Очистка HTML, нормализация
│
├── extractors/ # Извлечение структурированной информации
│ ├── init.py
│ ├── requirements_extractor.py # Извлечение требований
│ ├── responsibilities_extractor.py # Извлечение обязанностей
│ └── item_classifier.py # Классификация элементов
│
├── processors/ # Комплексная обработка данных
│ ├── init.py
│ └── description_processor.py # Обработка описаний (Facade)
│
├── analytics/ # Анализ данных
│ ├── init.py
│ └── analyzer.py # Анализ вакансий
│
├── storage/ # Сохранение данных
│ ├── init.py
│ └── savers.py # JSON, CSV сохранение
│
├── visualization/ # Визуализация результатов
│ ├── init.py
│ └── visualizer.py # Создание графиков
│
├── pipeline/ # Оркестрация процесса
│ ├── init.py
│ └── vacancy_pipeline.py # Главный pipeline (Facade)
│
├── config.py # Конфигурация приложения
├── main.py # Точка входа (DI фабрика)
├── requirements.txt # Зависимости
└── README.md # Документация
```


---

## 🏗️ Архитектурные принципы

### 1. **SOLID Principles**

#### **S - Single Responsibility Principle**
Каждый класс имеет одну зону ответственности:

| Класс | Ответственность |
|-------|-----------------|
| `VacancySearcher` | Только поиск вакансий через API |
| `VacancyDetailsFetcher` | Только получение детальной информации |
| `HtmlTextCleaner` | Только очистка HTML и нормализация текста |
| `RequirementsExtractor` | Только извлечение требований |
| `ResponsibilitiesExtractor` | Только извлечение обязанностей |
| `VacancyItemClassifier` | Только классификация элементов |
| `VacancyDescriptionProcessor` | Только координация обработки описаний |
| `VacancyAnalyzer` | Только анализ данных |
| `VacancyVisualizer` | Только визуализация |
| `JsonSaver/CsvSaver` | Только сохранение в определенном формате |
| `VacancyPipeline` | Только координация общего процесса |

#### **O - Open/Closed Principle**
Система открыта для расширения, закрыта для модификации:

```python
# ✅ Можно добавить новые реализации без изменения существующего кода

# Новый источник данных
class LinkedInVacancySearcher(IVacancySearcher):
    def search(self, query, ...):
        # Поиск в LinkedIn
        pass

# Новый формат сохранения
class ExcelSaver(IDataSaver):
    def save(self, data, filepath):
        data.to_excel(filepath)

# Новый способ очистки
class MarkdownTextCleaner(ITextCleaner):
    def clean(self, raw_text):
        # Очистка markdown
        pass

## Преимущества новой архитектуры:

✅ Легко тестировать (можно создать mock-объекты)  
✅ Легко расширять (новые форматы, источники данных)  
✅ Переиспользуемые компоненты  
✅ Четкое разделение ответственности  
✅ Гибкая конфигурация через DI

### 📊 Результаты работы:

После запуска для каждой вакансии создаются файлы:

```text
result/
└── python_разработчик/
    ├── raw.json                      # Сырые данные
    ├── processed.csv                 # Обработанные данные
    ├── skills.csv                    # Навыки
    ├── requirements.csv              # Требования
    ├── salary_stats.json             # Статистика ЗП
    ├── companies.csv                 # ⭐ НОВОЕ: Топ компаний
    ├── schedule.csv                  # ⭐ НОВОЕ: Форматы работы
    ├── metro.csv                     # ⭐ НОВОЕ: Станции метро
    ├── top_skills.png                # Графики
    ├── top_requirements.png
    ├── experience_distribution.png
    ├── top_companies.png             # ⭐ НОВОЕ: График компаний
    ├── schedule_distribution.png     # ⭐ НОВОЕ: График форматов
    └── top_metro.png                 # ⭐ НОВОЕ: График метро
```
#### **L - Liskov Substitution Principle**
Все реализации интерфейса взаимозаменяемы:
```python
# Можно заменить sync на async без изменения кода
searcher: IVacancySearcher = SyncVacancySearcher()
# или
searcher: IVacancySearcher = AsyncVacancySearcher()

# Pipeline работает с любой реализацией
pipeline = VacancyPipeline(searcher=searcher, ...)
```

#### **I - Interface Segregation Principle**
Интерфейсы специализированы и не заставляют реализовывать ненужное:

- IVacancySearcher - только поиск
- IVacancyDetailsFetcher - только получение деталей
- ITextCleaner - только очистка
- ITextSectionExtractor - только извлечение секций
- IDataSaver - только сохранение
- IVacancyAnalyzer - только анализ
- IVacancyVisualizer - только визуализация
- IDescriptionProcessor - только обработка описаний

#### **D - Dependency Inversion Principle**
Зависимость от абстракций, а не от конкретных реализаций:
```python
class VacancyPipeline:
    def __init__(
        self,
        searcher: IVacancySearcher,           # ← интерфейс
        details_fetcher: IVacancyDetailsFetcher,  # ← интерфейс
        analyzer_class: type,
        visualizer: IVacancyVisualizer        # ← интерфейс
    ):
        # Зависимость от абстракций, а не конкретных классов
        self.searcher = searcher
        self.details_fetcher = details_fetcher
        # ...
```

### 2. **Design Patterns**
#### **Facade Pattern**

Упрощение работы со сложной подсистемой:
```python
# VacancyPipeline - фасад для всей системы
pipeline = VacancyPipeline(searcher, fetcher, analyzer, visualizer)

# Простой интерфейс для сложного процесса
pipeline.process_single_query(query="Python Developer")

# VacancyDescriptionProcessor - фасад для обработки текста
processor = VacancyDescriptionProcessor(
    text_cleaner=cleaner,
    requirements_extractor=req_extractor,
    responsibilities_extractor=resp_extractor
)
processor.process_vacancies(vacancies)
```

#### Strategy Pattern
Выбор алгоритма в runtime:
```python
# Выбор стратегии парсинга
if config.PARSING_MODE == 'async':
    searcher = AsyncVacancySearcher()
else:
    searcher = SyncVacancySearcher()

# Выбор стратегии извлечения требований
if tech_keywords:
    extractor = SkillsBasedRequirementsExtractor(tech_keywords)
else:
    extractor = RequirementsExtractor()
```
#### Factory Pattern
Создание объектов через фабрику:
```python
def create_pipeline(config: Config) -> VacancyPipeline:
    """Фабрика для создания pipeline с нужными компонентами."""
    if config.PARSING_MODE == 'async':
        searcher = AsyncVacancySearcher(max_concurrent=config.MAX_CONCURRENT)
        details_fetcher = AsyncVacancyDetailsFetcher(max_concurrent=config.MAX_CONCURRENT)
    else:
        searcher = SyncVacancySearcher()
        details_fetcher = SyncVacancyDetailsFetcher()
    
    visualizer = VacancyVisualizer()
    
    return VacancyPipeline(
        searcher=searcher,
        details_fetcher=details_fetcher,
        analyzer_class=VacancyAnalyzer,
        visualizer=visualizer,
        output_dir=config.OUTPUT_DIR
    )
```
#### Dependency Injection
Внедрение зависимостей через конструктор:

```python
# Все зависимости передаются извне
pipeline = VacancyPipeline(
    searcher=searcher,              # ← инжектируется
    details_fetcher=details_fetcher, # ← инжектируется
    analyzer_class=VacancyAnalyzer,  # ← инжектируется
    visualizer=visualizer            # ← инжектируется
)

# Легко подменить на mock для тестирования
pipeline_test = VacancyPipeline(
    searcher=MockSearcher(),
    details_fetcher=MockFetcher(),
    analyzer_class=MockAnalyzer,
    visualizer=MockVisualizer
)
```
## 🔄 Поток данных
```text
┌─────────────────────────────────────────────────────────────────┐
│                         MAIN.PY                                 │
│  Точка входа - создание компонентов через DI фабрику           │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    VACANCY PIPELINE                             │
│  Координация всего процесса (Facade)                            │
└───────────┬───────────┬───────────┬───────────┬─────────────────┘
            │           │           │           │
            ▼           ▼           ▼           ▼
      ┌─────────┐ ┌─────────┐ ┌──────────┐ ┌────────────┐
      │Searcher │ │ Fetcher │ │ Analyzer │ │ Visualizer │
      └─────────┘ └─────────┘ └──────────┘ └────────────┘
            │           │           │           │
            ▼           ▼           ▼           ▼
      ┌─────────────────────────────────────────────────┐
      │  1. Поиск вакансий (краткая информация)         │
      └─────────────────┬───────────────────────────────┘
                        ▼
      ┌─────────────────────────────────────────────────┐
      │  2. Получение деталей по ID                     │
      └─────────────────┬───────────────────────────────┘
                        ▼
      ┌─────────────────────────────────────────────────┐
      │  3. Обработка описаний (опционально)            │
      │     ├── Очистка HTML (HtmlTextCleaner)          │
      │     ├── Извлечение требований (Extractor)       │
      │     ├── Извлечение обязанностей (Extractor)     │
      │     └── Классификация (Classifier)              │
      └─────────────────┬───────────────────────────────┘
                        ▼
      ┌─────────────────────────────────────────────────┐
      │  4. Анализ данных (VacancyAnalyzer)             │
      │     ├── Навыки                                  │
      │     ├── Требования                              │
      │     ├── Зарплаты                                │
      │     ├── Компании                                │
      │     ├── Форматы работы                          │
      │     └── Метро                                   │
      └─────────────────┬───────────────────────────────┘
                        ▼
      ┌─────────────────────────────────────────────────┐
      │  5. Сохранение (JsonSaver, CsvSaver)            │
      └─────────────────┬───────────────────────────────┘
                        ▼
      ┌─────────────────────────────────────────────────┐
      │  6. Визуализация (VacancyVisualizer)            │
      └─────────────────────────────────────────────────┘
```
---

## 📦 Модули и их назначение

### **core/** - Ядро системы

#### `interfaces.py`
Определяет контракты (интерфейсы) для всех компонентов системы:

- `IVacancySearcher` - поиск вакансий
- `IVacancyDetailsFetcher` - получение деталей
- `ITextCleaner` - очистка текста
- `ITextSectionExtractor` - извлечение секций
- `IDescriptionProcessor` - обработка описаний
- `IVacancyAnalyzer` - анализ данных
- `IVacancyVisualizer` - визуализация
- `IDataSaver` - сохранение данных

**Принципы:** Interface Segregation, Dependency Inversion

---

### **fetchers/** - Получение данных

#### `searcher.py`
Поиск вакансий через HH.ru API:

- `SyncVacancySearcher` - синхронный поиск (надежный)
- `AsyncVacancySearcher` - асинхронный поиск (быстрый)

**Возможности:**
- Постраничный поиск
- Ограничение количества вакансий
- Автоматические задержки
- Обработка rate limiting

#### `details_fetcher.py`
Получение детальной информации о вакансиях:

- `SyncVacancyDetailsFetcher` - синхронное получение
- `AsyncVacancyDetailsFetcher` - асинхронное получение с батчингом

**Возможности:**
- Пакетная обработка
- Retry-логика
- Обработка ошибок

**Принципы:** Single Responsibility, Liskov Substitution

---

### **parsers/** - Парсинг текста

#### `text_cleaner.py`
Очистка HTML и нормализация текста:

- `HtmlTextCleaner` - основной очиститель (BeautifulSoup)
- `SimpleTextCleaner` - упрощенный очиститель (regex)

**Возможности:**
- Удаление HTML-тегов
- Декодирование HTML-сущностей
- Нормализация пробелов
- Сохранение структуры документа

**Принципы:** Single Responsibility, Open/Closed

---

### **extractors/** - Извлечение информации

#### `requirements_extractor.py`
Извлечение требований к кандидату:

- `RequirementsExtractor` - базовый экстрактор
- `SkillsBasedRequirementsExtractor` - с фокусом на технические навыки

**Методы извлечения:**
1. Из размеченных секций ("Требования:", "Мы ожидаем:" и т.д.)
2. По ключевым маркерам ("опыт", "знание", "владение" и т.д.)
3. Из маркированных списков

**Фильтрация:**
- Удаление шума (маркетинг, условия работы)
- Фильтрация обязанностей
- Дедупликация похожих элементов

#### `responsibilities_extractor.py`
Извлечение обязанностей и задач:

- `ResponsibilitiesExtractor` - извлечение обязанностей

**Методы извлечения:**
1. Из размеченных секций ("Обязанности:", "Вам предстоит:" и т.д.)
2. По глагольным маркерам ("разработка", "внедрение", "анализ" и т.д.)
3. Из маркированных списков

**Фильтрация:**
- Удаление требований
- Удаление шума
- Дедупликация

#### `item_classifier.py`
Классификация элементов на требования/обязанности:

- `VacancyItemClassifier` - ML-подобный классификатор на паттернах

**Типы элементов:**
- `REQUIREMENT` - требование к кандидату
- `RESPONSIBILITY` - обязанность/задача
- `NOISE` - шум (маркетинг, условия)
- `UNKNOWN` - неопределенный

**Принципы:** Single Responsibility, Strategy

---

### **processors/** - Комплексная обработка

#### `description_processor.py`
Координация обработки описаний вакансий:

- `VacancyDescriptionProcessor` - фасад для обработки

**Процесс обработки:**
1. Очистка HTML → чистый текст
2. Извлечение требований → список требований
3. Извлечение обязанностей → список обязанностей
4. Классификация (опционально) → разделение смешанных элементов
5. Дедупликация → уникальные элементы
6. Частотный анализ → статистика

**Принципы:** Facade, Dependency Inversion

---

### **analytics/** - Анализ данных

#### `analyzer.py`
Анализ собранных данных о вакансиях:

- `VacancyAnalyzer` - основной анализатор

**Виды анализа:**
- Навыки (из key_skills)
- Требования (из описаний по ключевым словам)
- Статистика по зарплатам
- Топ компаний
- Распределение по опыту
- Форматы работы (график)
- Станции метро

**Принципы:** Single Responsibility

---

### **storage/** - Сохранение данных

#### `savers.py`
Сохранение результатов в различных форматах:

- `JsonSaver` - сохранение в JSON
- `CsvSaver` - сохранение в CSV
- `ExcelSaver` - сохранение в Excel (опционально)

**Принципы:** Single Responsibility, Open/Closed

---

### **visualization/** - Визуализация

#### `visualizer.py`
Создание графиков и диаграмм:

- `VacancyVisualizer` - основной визуализатор

**Типы графиков:**
- Топ навыков (bar chart)
- Топ требований (bar chart)
- Распределение по опыту (pie chart)
- Топ компаний (bar chart)
- Форматы работы (pie chart)
- Станции метро (bar chart)

**Принципы:** Single Responsibility

---

### **pipeline/** - Оркестрация

#### `vacancy_pipeline.py`
Главный координатор процесса:

- `VacancyPipeline` - фасад для всей системы

**Режимы работы:**
- `process_single_query()` - анализ одной должности
- `process_batch_queries()` - пакетный анализ

**Принципы:** Facade, Dependency Inversion

---
## 📊 Результаты работы

### Для каждой вакансии создается папка с файлами:

```
result/
└── бизнес_аналитик/
    │
    ├── 📄 Данные
    │   ├── raw.json                                  # Сырые данные от API
    │   ├── processed.csv                             # Структурированная таблица
    │   ├── skills.csv                                # Частота навыков
    │   ├── requirements.csv                          # Частота технологий
    │   ├── companies.csv                             # Топ компаний
    │   ├── schedule.csv                              # Форматы работы
    │   ├── metro.csv                                 # Станции метро
    │   │
    │   ├── 🆕 extracted_requirements_responsibilities.csv  # Извлеченные требования и обязанности
    │   ├── 🆕 requirements_frequency.csv                   # Частота требований
    │   ├── 🆕 responsibilities_frequency.csv               # Частота обязанностей
    │   │
    │   ├── salary_stats.json                         # Статистика зарплат
    │   ├── 🆕 detailed_extracted_data.json                 # Детальные данные извлечения
    │   └── 🆕 description_processing_stats.json            # Статистика обработки
    │
    └── 📈 Графики
        ├── top_skills.png                            # Топ-20 навыков
        ├── top_requirements.png                      # Топ-20 требований
        ├── experience_distribution.png               # Распределение по опыту
        ├── top_companies.png                         # Топ-15 компаний
        ├── schedule_distribution.png                 # Форматы работы
        └── top_metro.png                             # Топ-15 станций метро
```

### Для batch режима дополнительно:

```
result/
└── batch_summary.csv                                 # Сводная таблица по всем должностям
```

---
## 🎯 Преимущества архитектуры

### ✅ Тестируемость
```python
# Легко создать mock-объекты для тестирования
class MockSearcher(IVacancySearcher):
    def search(self, query, ...):
        return [{"id": "1", "name": "Test"}]

pipeline = VacancyPipeline(
    searcher=MockSearcher(),
    details_fetcher=MockFetcher(),
    analyzer_class=MockAnalyzer,
    visualizer=MockVisualizer
)
```

### ✅ Расширяемость
```python
# Добавление новых форматов без изменения существующего кода
class XmlSaver(IDataSaver):
    def save(self, data, filepath):
        # Сохранение в XML
        pass

# Добавление новых источников
class LinkedInSearcher(IVacancySearcher):
    def search(self, query, ...):
        # Поиск в LinkedIn
        pass
```

### ✅ Переиспользуемость
```python
# Компоненты можно использовать независимо
cleaner = HtmlTextCleaner()
clean_text = cleaner.clean(html_text)

extractor = RequirementsExtractor()
requirements = extractor.extract(clean_text)
```

### ✅ Гибкость конфигурации
```python
# Легко менять поведение через конфигурацию
if Config.PARSING_MODE == 'async':
    searcher = AsyncVacancySearcher()
else:
    searcher = SyncVacancySearcher()

if Config.USE_CLASSIFIER:
    processor = VacancyDescriptionProcessor(use_classifier=True)
```

### ✅ Понятная структура
- Четкое разделение ответственности
- Один класс = одна задача
- Легко найти нужный код
- Удобно поддерживать и развивать

---

## 🔧 Конфигурация

Все параметры настраиваются через `config.py`:

```python
class Config:
    # Режимы работы
    MODE: str = 'single'                    # 'single' или 'batch'
    PARSING_MODE: str = 'sync'              # 'sync' или 'async'
    
    # Параметры поиска
    MAX_VACANCIES_LIMIT: Optional[int] = 30
    MAX_PAGES_LIMIT: int = 20
    MAX_CONCURRENT: int = 10                # Для async режима
    
    # Обработка описаний
    USE_CLASSIFIER: bool = True             # 🆕 Классификатор
    SIMILARITY_THRESHOLD: float = 0.80      # 🆕 Порог дедупликации
    
    # Параметры извлечения требований
    REQ_MIN_LENGTH: int = 20                # 🆕
    REQ_MAX_LENGTH: int = 280               # 🆕
    REQ_MIN_WORDS: int = 3                  # 🆕
    
    # Параметры извлечения обязанностей
    RESP_MIN_LENGTH: int = 20               # 🆕
    RESP_MAX_LENGTH: int = 350              # 🆕
    RESP_MIN_WORDS: int = 4                 # 🆕
```

---

## 🚀 Расширение функциональности

### Добавление нового источника данных

```python
# 1. Создать класс, реализующий интерфейс
class SuperJobSearcher(IVacancySearcher):
    def search(self, query, area, max_pages, max_vacancies):
        # Реализация поиска в SuperJob
        pass

# 2. Зарегистрировать в фабрике
def create_pipeline(config):
    if config.SOURCE == 'superjob':
        searcher = SuperJobSearcher()
    else:
        searcher = SyncVacancySearcher()
    # ...
```

### Добавление нового формата сохранения

```python
# 1. Создать класс, реализующий интерфейс
class ParquetSaver(IDataSaver):
    def save(self, data: pd.DataFrame, filepath: str):
        data.to_parquet(filepath)

# 2. Использовать в pipeline
parquet_saver = ParquetSaver()
parquet_saver.save(df, 'result.parquet')
```

### Добавление нового типа анализа

```python
# 1. Расширить VacancyAnalyzer
class VacancyAnalyzer(IVacancyAnalyzer):
    # ... существующие методы ...
    
    def analyze_by_city(self) -> pd.DataFrame:
        """Новый метод анализа по городам."""
        if self.df is None:
            self.extract_data()
        
        city_counts = self.df['area'].value_counts().reset_index()
        city_counts.columns = ['Город', 'Количество']
        return city_counts
```
---
## 📚 Дополнительные ресурсы

- [HH.ru API Documentation](https://github.com/hhru/api)
- [SOLID Principles](https://en.wikipedia.org/wiki/SOLID)
- [Design Patterns](https://refactoring.guru/design-patterns)
- [Dependency Injection](https://en.wikipedia.org/wiki/Dependency_injection)

---


