"""
Модуль классификации элементов на требования и обязанности.
Использует паттерны и ключевые слова для разделения смешанных данных.
УЛУЧШЕННАЯ ВЕРСИЯ с расширенной фильтрацией шума.
"""

import re
from typing import List, Tuple
from enum import Enum


class ItemType(Enum):
    """Тип элемента."""
    REQUIREMENT = "requirement"
    RESPONSIBILITY = "responsibility"
    NOISE = "noise"
    UNKNOWN = "unknown"


class VacancyItemClassifier:
    """
    Классификатор элементов вакансий.

    Определяет, является ли элемент требованием, обязанностью или шумом.
    """

    # ========== ПАТТЕРНЫ ДЛЯ ШУМА ==========
    NOISE_PATTERNS = [
        # Информация о компании и достижениях
        r'\b(?:за|в течение)\s+\d+\s+(?:лет|года|год)',
        r'\bмы помогли\b',
        r'\b\d+\+?\s*компани[ий]',
        r'\bвнедрить\s+системное\s+управление',
        r'\bповысить\s+эффективность',
        r'\bсократить\s+затраты',
        r'\b(?:наша компания|мы являемся|мы занимаемся)',
        r'\bкомпания\s+(?:специализируется|работает|занимается)',

        # Что предлагает компания
        r'\b(?:мы предлагаем|we offer|что мы предлагаем)',
        r'\b(?:условия работы|условия|benefits)',
        r'\b(?:зарплата|salary|з/?п|оклад)',
        r'\b(?:от|до)\s+\d+\s*(?:000|тыс|тысяч|₽|руб)',
        r'\b(?:бенефиты|льготы|компенсаци)',
        r'\b(?:дмс|вмс|омс)',
        r'\b(?:корпоратив|тимбилдинг)',
        r'\b(?:профессиональный рост|карьерный рост)',
        r'\b(?:обучение|тренинги|курсы)\s+(?:за счет|бесплатн)',
        r'\b(?:комфортн[аы][яй]|удобн[аы][яй])\s+(?:офис|среда|атмосфера)',
        r'\b(?:гибкий график|удаленн[аы][яй] работ[аы]|гибрид)',
        r'\b(?:оформление|трудоустройство)',
        r'\b(?:соц[\.\s]*пакет|социальный пакет)',
        r'\b(?:максимальное влияние|влияние на результат)',
        r'\b(?:стабильная зарплата|стабильн[аы][яй] доход)',
        r'\b(?:адекватное|открытое)\s+(?:руководство|к диалогу)',

        # О компании
        r'\b(?:о компании|о нас|about us)',
        r'\b(?:наша цель|наша миссия|наше видение)',
        r'(?:^|\n)\s*мы\s+(?:являемся|занимаемся|создаем)',

        # Призывы и маркетинг
        r'\b(?:если вы хотите|если (?:тебе|вам) важно)',
        r'\b(?:будем рады|ждем (?:вас|тебя)|присоединяйтесь)',
        r'\b(?:откликайтесь|звоните|пишите)',
        r'📩|📧|✉️|💼|🎯|🚀|⭐|✨',

        # Общие слишком широкие фразы
        r'^\s*(?:уникальн[аы][яй]|отличн[аы][яй]|прекрасн[аы][яй])',
        r'\b(?:возможность|перспективы)\s+(?:развития|роста|карьеры)',
        r'^мы рассматриваем кандидатов',
        r'^идеальный кандидат',
        r'^вы нам подходите',

        # Форматы работы и график
        r'^\s*(?:работа в|формат|график)',
        r'^\s*(?:полностью\s+)?удален[нн][ыо][йе]',
        r'^\s*\d+/\d+',  # 5/2
        r'^\s*офис',
        r'^\s*локация',
    ]

    # Паттерны для ТРЕБОВАНИЙ
    REQUIREMENT_PATTERNS = [
        # Опыт работы
        r'\b(?:опыт|experience)\s+(?:работы|от|не менее|более|\d+)',
        r'\bот\s+\d+\s+(?:лет|года|год)\b',
        r'\b\d+\+?\s+(?:лет|года|год)\s+(?:опыт|работы)',

        # Знания и навыки
        r'\b(?:знание|знания|knowledge of)\s+',
        r'\b(?:владение|proficiency|mastery)\s+',
        r'\b(?:умение|навык[ии]?|skill[s]?|ability)\s+',
        r'\b(?:понимание|understanding)\s+(?:принципов|основ|технологий)',
        r'\bнавык[ии]?\s+(?:работы с|использования|применения)',

        # Образование
        r'\b(?:высшее|среднее)\s+образование',
        r'\b(?:диплом|степень|образование)\s+(?:в области|по направлению)',
        r'\bобразование\s+(?:\(|в)',

        # Технические навыки
        r'\b(?:уверенн[��ы][ей]|глубок[оие][ей])\s+(?:знание|владение|понимание)',
        r'\b(?:базов[оы][ей]|продвинут[оы][ей])\s+(?:знание|владение|уровень)',
        r'\b(?:sql|python|java|excel|jira|confluence)\b',

        # Языки
        r'\b(?:english|английский)\s+(?:язык|не ниже|от|на уровне)',
        r'\b(?:intermediate|advanced|upper-intermediate|b\d|c\d)\b',

        # Сертификаты и квалификация
        r'\b(?:сертификат|certification)',
        r'\b(?:must have|nice to have|required|preferred)\b',

        # Личные качества как требования
        r'\b(?:желател[ьн]|обязател[ьн]|необходим[оы])\b',
        r'\b(?:будет плюсом|плюсом будет|преимуществом)',
        r'\b(?:аналитическ[иоа][ейм]|системн[оа][ейм])\s+(?:мышление|склад ума)',
        r'\b(?:коммуникативн|коммуникабельн)',
        r'\b(?:внимательн[оы][ей]|ответственн[оы][ей])',

        # Специфические требования
        r'\b(?:имеет|имеешь)\s+(?:опыт|навык)',
        r'\b(?:грамотн[аы][яй])\s+(?:устная|письменная)\s+речь',
        r'\b(?:релевантный|подтвержденный)\s+опыт',

        # Методологии и подходы
        r'\b(?:bpmn|uml|idef|epc|dfd|erd)\b',
        r'\b(?:agile|scrum|kanban|waterfall)\b',

        # Инструменты
        r'\b(?:битрикс|bitrix|1c|erp|crm|tms|wms)\b',
        r'\b(?:visio|business studio|aris|miro|notion)\b',
        r'\b(?:tableau|power\s*bi|qlik|looker|datalens)\b',
    ]

    # Паттерны для ОБЯЗАННОСТЕЙ
    RESPONSIBILITY_PATTERNS = [
        # Действия и процессы
        r'\b(?:разработка|разработать|develop|build|create)\b',
        r'\b(?:проектирование|проектировать|design)\b',
        r'\b(?:внедрение|внедрить|implement|deploy)\b',
        r'\b(?:поддержка|поддерживать|maintain|support)\s+(?:систем|продукт|приложени|пользовател)',
        r'\b(?:оптимизация|оптимизировать|optimize)\b',
        r'\b(?:автоматизация|автоматизировать)\b',

        # Участие и взаимодействие
        r'\b(?:участие в|participation in)\b',
        r'\b(?:работа с|working with)\s+(?:команд|данным|клиент)',
        r'\b(?:взаимодействие|collaborate|interaction)\s+(?:с|with)',

        # Создание и подготовка
        r'\b(?:создание|создавать)\s+(?:документ|отчет|решени)',
        r'\b(?:подготовка|формирование|составление)\s+(?:отчет|документ|план)',
        r'\b(?:описание|документирование|формализация)\b',

        # Анализ и сбор
        r'\b(?:анализ|анализировать|analyze)\s+(?:данных|процесс|требовани)',
        r'\b(?:сбор|собирать|collect)\s+(?:требовани|данных|информаци)',
        r'\b(?:выявление|выявлять|идентификация)\s+(?:требовани|проблем)',

        # Контроль и мониторинг
        r'\b(?:контроль|мониторинг|отслеживание)\b',
        r'\b(?:проверка|тестирование|test)\s+(?:функци|процесс|систем)',

        # Специфические действия
        r'\b(?:обучение|training)\s+(?:пользователей|сотрудников)',
        r'\b(?:настройка|configure|setup)\b',
        r'\b(?:интеграция|integrate)\b',
        r'\b(?:консультирование|консультаци)\b',

        # Формулировки обязанностей
        r'\b(?:you will|вам предстоит|чем заниматься|чем предстоит)\b',
        r'\b(?:какие задачи|задачи вам|ваши обязанности)\b',
        r'^\s*(?:поиск|ведение|обеспечение)\s+',

        # Работа с конкретными объектами
        r'\b(?:построение|разработка)\s+(?:дашборд|отчет|презентаци)',
        r'\b(?:ведение|актуализация)\s+(?:баз|документ|реестр)',
    ]

    # Стоп-слова для требований (если есть - это НЕ требование)
    REQUIREMENT_STOP_WORDS = [
        r'\b(?:разрабатывать|внедрять|проектировать|создавать)\b',
        r'\b(?:предстоит|будете|заниматься)\b',
        r'\b(?:вам предстоит|чем предстоит)\b',
        r'^\s*(?:поиск|анализ|работа)\s+(?:и|новых|категорий)',
        r'^\s*(?:сбор|формирование|подготовка)\s+(?:обратной|отчет)',
    ]

    # Стоп-слова для обязанностей (если есть - это НЕ обязанность)
    RESPONSIBILITY_STOP_WORDS = [
        r'\b(?:опыт работы от|опыт от)\s+\d+',
        r'\b(?:опыт|знание|владение|умение)\s+(?:работы с|использования)',
        r'\b(?:образование|степень|диплом)\b',
        r'\b(?:высшее|среднее)\s+образование',
        r'\b(?:английский|english)\s+(?:язык|не ниже)',
        r'\b(?:релевантный|желателен|обязателен)\s+опыт',
        r'\b(?:технические знания|техническая насмотренность)\b',
        r'\b(?:базовые основы|понимание основ)\b',
        r'\b(?:frontend|backend|sql)\b',
        r'(?:будет\s+)?(?:преимуществом|плюсом)',
    ]

    def __init__(self):
        """Инициализация классификатора."""
        self._compile_patterns()

    def _compile_patterns(self):
        """Компиляция регулярных выражений."""
        self.noise_patterns = [
            re.compile(p, re.IGNORECASE | re.UNICODE)
            for p in self.NOISE_PATTERNS
        ]

        self.req_patterns = [
            re.compile(p, re.IGNORECASE | re.UNICODE)
            for p in self.REQUIREMENT_PATTERNS
        ]

        self.resp_patterns = [
            re.compile(p, re.IGNORECASE | re.UNICODE)
            for p in self.RESPONSIBILITY_PATTERNS
        ]

        self.req_stop_patterns = [
            re.compile(p, re.IGNORECASE | re.UNICODE)
            for p in self.REQUIREMENT_STOP_WORDS
        ]

        self.resp_stop_patterns = [
            re.compile(p, re.IGNORECASE | re.UNICODE)
            for p in self.RESPONSIBILITY_STOP_WORDS
        ]

    def classify(self, text: str) -> ItemType:
        """
        Классификация элемента.

        Args:
            text: Текст для классификации

        Returns:
            ItemType (REQUIREMENT, RESPONSIBILITY, NOISE или UNKNOWN)
        """
        if not text or len(text.strip()) < 10:
            return ItemType.UNKNOWN

        text_clean = text.strip()

        # ПРИОРИТЕТ 1: Проверка на шум
        if self._is_noise(text_clean):
            return ItemType.NOISE

        # ПРИОРИТЕТ 2: Классификация на требование/обязанность
        req_score = self._calculate_score(text_clean, self.req_patterns)
        resp_score = self._calculate_score(text_clean, self.resp_patterns)

        # Проверка стоп-слов
        if self._has_stop_words(text_clean, self.req_stop_patterns):
            req_score = max(0, req_score - 2)

        if self._has_stop_words(text_clean, self.resp_stop_patterns):
            resp_score = max(0, resp_score - 2)

        # Принятие решения
        if req_score > resp_score and req_score > 0:
            return ItemType.REQUIREMENT
        elif resp_score > req_score and resp_score > 0:
            return ItemType.RESPONSIBILITY
        else:
            return ItemType.UNKNOWN

    def _is_noise(self, text: str) -> bool:
        """Проверка, является ли текст шумом."""
        for pattern in self.noise_patterns:
            if pattern.search(text):
                return True

        # Дополнительные эвристики
        # Слишком короткие или слишком длинные
        if len(text) < 15 or len(text) > 500:
            return True

        # Начинается с маркетинговых слов
        marketing_starts = [
            'уникальн', 'отличн', 'прекрасн', 'превосходн',
            'мы предлагаем', 'что мы', 'наша компания', 'наша цель',
            'если вы', 'если тебе', 'ждем', 'будем рады',
            'за 15 лет', 'за 10 лет', 'мы помогли',
            'максимальное влияние', 'работа в постоянно',
            'стабильная зарплата', 'адекватное',
        ]

        text_lower = text.lower()
        for word in marketing_starts:
            if text_lower.startswith(word):
                return True

        # Проверка на информацию о компании
        company_info_patterns = [
            r'помогли\s+\d+\+?\s*компани',
            r'внедрить\s+системное\s+управление',
            r'компания\s+(?:работает|специализируется)',
        ]

        for pattern in company_info_patterns:
            if re.search(pattern, text_lower):
                return True

        return False

    def _calculate_score(self, text: str, patterns: List[re.Pattern]) -> int:
        """Подсчет количества совпадений с паттернами."""
        score = 0
        for pattern in patterns:
            matches = pattern.findall(text)
            score += len(matches)
        return score

    def _has_stop_words(self, text: str, patterns: List[re.Pattern]) -> bool:
        """Проверка наличия стоп-слов."""
        for pattern in patterns:
            if pattern.search(text):
                return True
        return False

    def separate_mixed_items(
            self,
            items: List[str]
    ) -> Tuple[List[str], List[str]]:
        """
        Разделение смешанного списка на требования и обязанности.

        Args:
            items: Список элементов для разделения

        Returns:
            Кортеж (требования, обязанности)
        """
        requirements = []
        responsibilities = []

        for item in items:
            item_type = self.classify(item)

            if item_type == ItemType.REQUIREMENT:
                requirements.append(item)
            elif item_type == ItemType.RESPONSIBILITY:
                responsibilities.append(item)
            # NOISE и UNKNOWN элементы игнорируются

        return requirements, responsibilities
